defaults: # for loading the default config
  - training: default
  - trainer: default
  - optimizer: adamw
  - scheduler: linear_schedule_with_warmup
  - tokenizer: huggingface/default
  - backbone: huggingface/default
  - logger: tchaton # todo: change this eventually

task: ???
dataset: ???
experiment_name: ${now:%Y-%m-%d}_${now:%H-%M-%S}
log: False
ignore_warnings: True # todo: check warnings before release